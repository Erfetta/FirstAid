<!DOCTYPE html>
<html lang="it">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Chat tra Utente e Operatore</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
      background-color: #f4f7fc;
    }
    .chat-container {
      width: 80%;
      max-width: 900px;
      margin: 20px auto;
      border: 1px solid #ddd;
      border-radius: 8px;
      background-color: white;
    }
    .chat-header {
      background-color: #3498db;
      color: white;
      padding: 15px;
      text-align: center;
      font-size: 18px;
      border-radius: 8px 8px 0 0;
    }
    .chat-box {
      height: 400px;
      overflow-y: auto;
      padding: 10px;
      border-bottom: 1px solid #ddd;
      max-height: 500px;
    }
    .message {
      padding: 8px;
      margin-bottom: 10px;
      border-radius: 5px;
      background-color: #f1f1f1;
    }
    .my-message {
      background-color: #d1f7d1;
      text-align: right;
    }
    .operator-message {
      background-color: #f7d1d1;
    }
    .input-box {
      display: flex;
      padding: 10px;
      border-top: 1px solid #ddd;
    }
    .input-box input {
      width: 85%;
      padding: 10px;
      border: 1px solid #ddd;
      border-radius: 5px;
    }
    .input-box button {
      width: 15%;
      background-color: #3498db;
      color: white;
      padding: 10px;
      border: none;
      border-radius: 5px;
      cursor: pointer;
    }
    .input-box button:hover {
      background-color: #2980b9;
    }
  </style>
</head>
<body>

<div class="chat-container">
  <div class="chat-header">Chat con l'Operatore</div>
  <div class="chat-box">
    <!-- I messaggi verranno aggiunti dinamicamente tramite il backend -->
    <div class="message my-message">
      Ciao, come posso aiutarti?
    </div>
    <div class="message operator-message">
      Salve, ho bisogno di assistenza riguardo un problema tecnico.
    </div>
  </div>
  <div class="input-box">
    <input type="text" id="message-input" placeholder="Scrivi un messaggio..." />
    <button id="send-message">Invia</button>
  </div>
</div>


<%= link_to 'Torna alla Home', root_path, class: 'button' %>
</body>
</html>
